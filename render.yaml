services:
  - type: worker
    name: dynamic-quiz-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python quizbot_clone.py
    envVars:
      - key: DATABASE_DIR
        value: /data
    disk:
      name: quiz-storage-disk
      mountPath: /data
      sizeGB: 1
      
